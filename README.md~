#Twenty Question Game
by: Guelor Emanuel

Brief Info:(https://en.wikipedia.org/wiki/Twenty_Questions)
--------------------
- client server model structure and TCP/IP communications protocols on Linux Ubuntu 
  
  ```Applied the client server model structure and TCP/IP communications protocols on Linux Ubuntu 
  distribution to implement the game of Twenty Questions using C, where the answerer answers with UNIX
  signals and the hosts can switch between answerer and questioner (server and client) depending 
  on who won the round.```

# Compiling/Running instructions:
0. using linux shell navigate to the root directory of the project:
    ```Type the following command "make"```
1. For the oracle run: ./node

  ```./node```
2. Open a new terminal and run ./node 127.0.0.1:

  ```./node 127.0.0.1```
